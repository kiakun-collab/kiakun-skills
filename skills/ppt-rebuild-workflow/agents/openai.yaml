interface:
  display_name: "PPT Rebuild Workflow"
  short_description: "Rebuild reference slides into editable PPTX."
  default_prompt: "Use $ppt-rebuild-workflow to rebuild these slides. Validate semantics first, then run computed reference/render and typography calibration before accepting Mode B/C QA."
