interface:
  display_name: "GPT Image 2 API"
  short_description: "XApex-first async images with tiered fallback routing"
  default_prompt: "Use $gpt-image-2-api to generate or edit an image through XApex async first, with AtlasCloud and aifast fallbacks when supported."
